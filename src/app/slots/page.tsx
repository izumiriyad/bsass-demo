import type { Metadata } from "next";
import { SlotsPage } from "@/components/bj88/category-pages";

export const metadata: Metadata = {
  title: "Slots",
};

export default function Page() {
  return <SlotsPage />;
}
